# Core Web Framework
Flask==3.1.2                # The main web framework for the application
Werkzeug==3.1.4             # WSGI utility library (Flask dependency)
Jinja2==3.1.6               # Templating engine (Flask dependency)
itsdangerous==2.2.0         # Secure data signing (Flask dependency)
click==8.3.1                # CLI tool creation (Flask dependency)
blinker==1.9.0              # Signal support (Flask dependency)
MarkupSafe==3.0.3           # XML/HTML safe strings (Jinja2 dependency)

# Machine Learning & Data Science
scikit-learn==1.7.2         # Machine learning library (preprocessing, evaluation)
xgboost==2.0.3              # Gradient boosting library (SHAP-compatible version)
imbalanced-learn==0.14.0    # SMOTE and sampling utilities
shap==0.46.0                # Explainable AI (SHAP values, compatible with XGBoost 2.0.3)
numpy==1.26.4               # Numerical computing (arrays, math, <2.0 for compatibility)
pandas==2.3.3               # Data manipulation and analysis (DataFrames)
scipy==1.16.3               # Scientific computing (stats, optimization)
joblib==1.5.2               # Pipeline serialization (saving/loading models)

# Visualization
matplotlib==3.10.7          # Plotting library (static charts)
contourpy==1.3.3            # Contour calculation (Matplotlib dependency)
cycler==0.12.1              # Style cycles (Matplotlib dependency)
fonttools==4.61.0           # Font manipulation (Matplotlib dependency)
kiwisolver==1.4.9           # Constraint solver (Matplotlib dependency)
pillow==12.0.0              # Image processing (Matplotlib dependency)
pyparsing==3.2.5            # Parsing library (Matplotlib dependency)

# Utilities & Dependencies
tqdm==4.67.1                # Progress bars
ruff==0.14.7                # Linter for code quality
python-dateutil==2.9.0.post0 # Date extensions (Pandas dependency)
pytz==2025.2                # Timezone definitions (Pandas dependency)
tzdata==2025.2              # Timezone data (Pandas dependency)
packaging==25.0             # Core utilities for Python packages
six==1.17.0                 # Python 2/3 compatibility utility
typing_extensions==4.15.0   # Backported typing features
threadpoolctl==3.6.0        # Thread-pool controls (sklearn/numpy dependency)
cloudpickle==3.1.2          # Extended pickling support (joblib/sklearn dependency)
numba==0.62.1               # JIT compiler (SHAP dependency)
llvmlite==0.45.1            # LLVM binding (Numba dependency)
slicer==0.0.8               # Slicing utilities (SHAP dependency)
